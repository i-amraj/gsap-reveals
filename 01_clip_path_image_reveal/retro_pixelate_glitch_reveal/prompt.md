# 🤖 AI Master Prompt: Retro Pixelate Glitch Reveal Engine (`retro_pixelate_glitch_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a cyberpunk retro 8-bit pixel glitch matrix image reveal section (`retro_pixelate_glitch_reveal`) using HTML5 Canvas composite masking.

Requirements:
1. Cyberpunk dark magenta/violet theme container with dark overlay and centered title text.
2. Top HD image hidden from DOM (`display: none`) and composited inside offscreen pixel glitch blocks using `destination-in`.
3. Mouse move activates grid blocks with randomized horizontal glitch jitter shifts, animating opacity fade-out.
4. Render loop executed via requestAnimationFrame with window resize grid rebuilding.
5. Provide modular HTML, CSS, and pure JS implementation.
```
