# 🤖 AI Master Prompt: Glassmorphic Cracked Frost Ice Break Reveal Engine (`cracked_frost_ice_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a glassmorphic shattered frost ice break image reveal section (`cracked_frost_ice_reveal`) using HTML5 Canvas composite masking.

Requirements:
1. Frost ice cyan/dark blue theme container with dark overlay and centered badge text.
2. Top HD image hidden from DOM (`display: none`) and composited inside offscreen jagged ice shard polygons using `destination-in`.
3. Mouse move spawns `IceShard` objects with randomized crystalline vertex polygons (`points array`) and radius expansion.
4. Smooth 60FPS requestAnimationFrame render loop with canvas resize support.
5. Provide clean modular HTML, CSS, and pure JS files.
```
