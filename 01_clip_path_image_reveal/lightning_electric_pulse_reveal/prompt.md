# 🤖 AI Master Prompt: Lightning Electric Pulse Reveal Engine (`lightning_electric_pulse_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a high-voltage plasma electric lightning pulse image reveal section (`lightning_electric_pulse_reveal`) using HTML5 Canvas composite masking.

Requirements:
1. Electric blue/dark violet theme viewport with ambient background layer and centered title text.
2. Top HD image hidden from DOM (`display: none`) and rendered strictly inside electric plasma arc paths using `destination-in`.
3. Mouse move spawns `ElectricArc` particles with jagged bolt branch lines (`branches array`) and glowing radial aura gradients.
4. Render loop driven by requestAnimationFrame with canvas resize support.
5. Provide complete self-contained HTML, CSS, and JS files.
```
