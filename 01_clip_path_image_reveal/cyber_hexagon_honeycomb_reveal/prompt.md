# 🤖 AI Master Prompt: Cyber Hexagon Honeycomb Matrix Reveal Engine (`cyber_hexagon_honeycomb_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a sci-fi cyber hexagonal honeycomb grid scanner image reveal section (`cyber_hexagon_honeycomb_reveal`) using HTML5 Canvas composite masking.

Requirements:
1. Futuristic dark cyan/blue layout with ambient background image and centered title text.
2. Top HD image hidden from DOM (`display: none`) and rendered strictly inside activated hexagonal grid cells using `destination-in`.
3. Mouse move activates grid cells within proximity radius (`drawHexagon` with 6-point vertex geometry), animating opacity fade-out.
4. Render loop executed via requestAnimationFrame with window resize listener rebuilding the grid.
5. Provide complete self-contained HTML, CSS, and JS files.
```
