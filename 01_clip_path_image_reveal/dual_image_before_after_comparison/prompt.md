# 🤖 AI Master Prompt: Dual Image Black & White to Full Color Reveal (`dual_image_before_after_comparison`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Before/After image comparison showcase using GSAP ScrollTrigger clip-path circle reveal (`dual_image_before_after_comparison`).

Requirements:
1. Base image layer styled with CSS `filter: grayscale(100%) brightness(0.4)`.
2. Top image layer positioned absolutely over the base image with full vibrant colors.
3. Top image animated from `clip-path: circle(0% at 50% 50%)` to `clip-path: circle(100% at 50% 50%)` bound to scroll scrub.
4. Clean HTML5 structure, CSS styling, and GSAP JavaScript code.
```
