# 🤖 AI Master Prompt: 60FPS Fluid Smoke Cloud Reveal Engine (`interactive_mouse_spotlight_torch`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Build a high-performance 60FPS fluid smoke particle mask image reveal engine (`interactive_mouse_spotlight_torch`) using HTML5 Offscreen Canvas Compositing.

Requirements:
1. Base ambient background image with dark overlay and centered badge text.
2. Top HD image hidden from DOM (`display: none`) and composited inside an offscreen canvas using `destination-in`.
3. Mouse movement spawns `SmokeParticle` objects that swell, drift upwards (`vy < 0`), and fade out organically.
4. Render loop executed via requestAnimationFrame with automatic canvas resize handlers.
5. Provide modular HTML, CSS, and pure JS implementation.
```
