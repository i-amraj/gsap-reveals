# 🤖 AI Master Prompt: Kinetic Smoke Ring Blast Shockwave Reveal Engine (`smoke_ring_shockwave_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a kinetic shockwave blast ring image reveal section (`smoke_ring_shockwave_reveal`) using HTML5 Canvas composite masking.

Requirements:
1. Action emerald/teal theme viewport container with ambient background image and centered title text overlay.
2. Top HD image hidden from DOM (`display: none`) and composited inside expanding shockwave blast rings using `destination-in`.
3. Mouse click / Mouse move spawns `ShockwaveRing` objects with radius expansion (`radius += growth`), dynamic stroke width, and hollow center.
4. Smooth 60FPS requestAnimationFrame loop with automatic canvas resize handling.
5. Provide complete self-contained HTML, CSS, and JS files.
```
