# 🤖 AI Master Prompt: Water Ripple Liquid Flow Reveal Engine (`water_ripple_liquid_flow_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a liquid water ripple mask image reveal section (`water_ripple_liquid_flow_reveal`) using HTML5 Canvas `destination-in` compositing.

Requirements:
1. Full-screen section with ambient blurred background image and centered title text overlay.
2. Top HD image hidden from DOM (`display: none`) and composited inside dynamic offscreen water ripple shapes.
3. Mouse move spawns `WaterRipple` particles with expanding radial gradient rings.
4. Smooth 60FPS requestAnimationFrame render loop with window resize listener.
5. Provide complete HTML, CSS, and pure JS files.
```
