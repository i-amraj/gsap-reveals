# 🤖 AI Master Prompt: Artistic Ink Splatter Reveal Engine (`artistic_ink_splatter_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an artistic Japanese ink splatter paint brush image reveal section (`artistic_ink_splatter_reveal`) using HTML5 Canvas composite masking.

Requirements:
1. Luxury dark fashion theme viewport with ambient background layer and centered text overlay.
2. Top HD image hidden from DOM (`display: none`) and rendered strictly inside offscreen wet ink splatter shapes using `destination-in`.
3. Mouse move spawns `InkSplatter` particles with main drop radius and surrounding smaller droplet dots (`dots array`).
4. Smooth 60FPS requestAnimationFrame loop with automatic canvas resize handling.
5. Provide complete self-contained HTML, CSS, and JS files.
```
