# 🤖 AI Master Prompt: Fire Embers Vaporize Reveal Engine (`fire_embers_vaporize_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a fiery burning embers image reveal effect (`fire_embers_vaporize_reveal`) using HTML5 Canvas particle composite masking.

Requirements:
1. Dark red/black gaming theme container with dark overlay and centered badge text.
2. Top HD image hidden from DOM (`display: none`) and composited inside offscreen fire ember particles using `destination-in`.
3. Mouse move spawns `FireEmber` particles with upward acceleration (`vy < 0`), spinning rotation, and radial ember glow gradients.
4. Smooth 60FPS requestAnimationFrame loop with canvas auto-resizing.
5. Provide clean HTML, CSS, and JS modular files.
```
