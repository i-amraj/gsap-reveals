# 🤖 AI Master Prompt: Magic Dust & Star Sparkle Wand Reveal Engine (`magic_dust_sparkle_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a magic dust star sparkle wand image reveal section (`magic_dust_sparkle_reveal`) using HTML5 Canvas particle composite masking.

Requirements:
1. Luxury dark purple/gold viewport container with dark overlay and centered badge text.
2. Top HD image hidden from DOM (`display: none`) and composited inside offscreen magic dust particles using `destination-in`.
3. Mouse move spawns `MagicParticle` objects with twirling rotation, velocity expansion, and soft radial aura gradients.
4. Render loop driven by requestAnimationFrame with window resize listener.
5. Provide clean modular HTML, CSS, and pure JS implementation.
```
