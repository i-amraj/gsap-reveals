# 🤖 AI Master Prompt: Vertical Curtain Split Reveal (`curtain_split_inset_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a modern web Hero section with a GSAP ScrollTrigger vertical curtain split image reveal (`curtain_split_inset_reveal`).

Requirements:
1. Pinned full-bleed viewport container with dark background ambient text overlay.
2. Top image animated from `clip-path: inset(50% 0% 50% 0%)` to `clip-path: inset(0% 0% 0% 0%)`.
3. GSAP ScrollTrigger configured with `scrub: 1` and `pin: true` for smooth 60FPS scroll binding.
4. Clean HTML5, CSS3, and JavaScript modular structure.
```
