# 🤖 AI Master Prompt: Heat Wave Mirage Shimmer (`heat_wave_shimmer`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Cinematic / Automotive style Heat Wave Mirage Shimmer animation (`heat_wave_shimmer`) using SVG Filters and GSAP 3.

Requirements:
1. Image filtered with `<feTurbulence>` (high `baseFrequency="0.05 0.1"`) and `<feDisplacementMap>`.
2. Continuously animate the `baseFrequency` or `<animate>` tag inside SVG to create a constant shimmering mirage effect.
3. On mouseenter, GSAP increases the displacement map `scale` from 10 to 35 to intensify the heat wave distortion.
4. On mouseleave, it relaxes back to a scale of 10.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
