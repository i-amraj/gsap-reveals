# 🤖 AI Master Prompt: Frosted Condensation Wipe (`frosted_condensation_wipe`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Luxury style Frosted Condensation Wipe animation (`frosted_condensation_wipe`) using CSS Masks and GSAP 3.

Requirements:
1. Card container holds a base image (`nature1.jpeg`).
2. An overlay element covers the image with a heavy `backdrop-filter: blur(15px) brightness(0.8)` to simulate frosted condensation glass.
3. Apply a CSS `mask-image` (radial-gradient) to this frosted overlay.
4. On mousemove, use GSAP `quickTo` to smoothly move the center of the mask-image radial-gradient to follow the cursor.
5. On mouseenter, expand the mask transparent circle size to "wipe away" the condensation under the cursor. Shrink it back on mouseleave.
6. Provide complete self-contained HTML5, CSS3, and JS files.
```
