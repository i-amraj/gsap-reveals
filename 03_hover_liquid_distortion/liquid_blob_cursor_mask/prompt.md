# 🤖 AI Master Prompt: Liquid Blob Cursor Mask (`liquid_blob_cursor_mask`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Modern Digital Agency style Liquid Blob Cursor Mask animation (`liquid_blob_cursor_mask`) using SVG Gooey Filters and GSAP 3.

Requirements:
1. Two stacked images (`nature1.jpeg` base, `nature2.jpeg` overlay).
2. The overlay image is clipped/masked by an organic blob that follows the mouse cursor.
3. The blob container uses an SVG Gooey filter (`<feGaussianBlur>` and `<feColorMatrix>`) with multiple inner moving circles to create a fluid, organic shape-shifting feel.
4. GSAP `quickTo` is used for smooth mouse tracking performance.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
