# 🤖 AI Master Prompt: Magnetic Fluid Drag (`magnetic_fluid_drag`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Creative Portfolio style Magnetic Fluid Drag animation (`magnetic_fluid_drag`) using SVG Filters and GSAP 3.

Requirements:
1. Card image filtered using an SVG `<feTurbulence>` and `<feDisplacementMap>`.
2. As the user moves the mouse over the card, calculate the relative cursor coordinates.
3. Use GSAP `quickTo` to smoothly animate the `baseFrequency` or turbulence seed based on mouse X/Y position, creating a liquid "drag" or "smear" effect that follows the cursor.
4. Scale up the displacement `scale` on hover.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
