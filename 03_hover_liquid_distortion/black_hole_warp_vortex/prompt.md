# 🤖 AI Master Prompt: Black Hole Warp Vortex (`black_hole_warp_vortex`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Sci-Fi style Black Hole Warp Vortex animation (`black_hole_warp_vortex`) using SVG Filters and GSAP 3.

Requirements:
1. Image filtered with an SVG `<feTurbulence>` (radial or standard) and `<feDisplacementMap>`.
2. On mouseenter, GSAP animates the displacement map `scale` to an extreme value (e.g., negative or very high) while shrinking the image scale via CSS transforms (e.g., `scale(0.7)`) to simulate the pixels being sucked inward.
3. On mouseleave, smoothly snap the displacement and transform scale back to default.
4. Provide complete self-contained HTML5, CSS3, and JS files.
```
