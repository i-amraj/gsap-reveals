# 🤖 AI Master Prompt: Pixel Sort Glitch Melt (`pixel_sort_melt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Cyberpunk style Pixel Sort Glitch Melt animation (`pixel_sort_melt`) using SVG Filters and GSAP 3.

Requirements:
1. Image filtered with `<feTurbulence>` (where X frequency is high and Y frequency is low, e.g., `baseFrequency="0.1 0.001"`) to create vertical stretching bands.
2. Use `<feDisplacementMap>` mapped primarily to the Y-axis.
3. On mouseenter, GSAP increases the displacement map `scale` from 0 to 100, creating a vertical dripping/melting pixel sort effect.
4. On mouseleave, smoothly transition the scale back to 0.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
