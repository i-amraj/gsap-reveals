# 🤖 AI Master Prompt: Crystal Prism Shatter (`crystal_prism_shatter`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a High-End Fashion style Crystal Prism Shatter animation (`crystal_prism_shatter`) using GSAP 3.

Requirements:
1. Card container holding one base image and several duplicate image layers (`fragments`).
2. Each fragment uses a unique CSS `clip-path: polygon(...)` to form jagged, crystal-like shards covering different parts of the image.
3. On mouseenter, GSAP explodes the shards slightly outward (translating X/Y away from center and scaling up). Add a backdrop filter or opacity shift to simulate glass reflection.
4. On mouseleave, the shards smoothly snap back together into a solid image.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
