# 🤖 AI Master Prompt: Directional Fluid Flow Morphing Hover (`directional_wave_slide`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Fashion Editorial style Directional Fluid Flow Morphing Hover animation (`directional_wave_slide`) using GSAP 3 and SVG Filters.

Requirements:
1. Two stacked images (`nature1.jpeg` base, `nature2.jpeg` hover overlay).
2. Hover overlay clips in directionally based on mouse entry angle (Left to Right, Top to Bottom).
3. SVG `<feDisplacementMap>` triggers a momentary liquid wave ripple as the image transition slides through.
4. On mouseleave, the overlay retracts smoothly back to its origin.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
