# 🤖 AI Master Prompt: Sonic Boom Shockwave (`sonic_boom_shockwave`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Action / Sports style Sonic Boom Shockwave animation (`sonic_boom_shockwave`) using SVG Filters and GSAP 3.

Requirements:
1. Image filtered with an SVG `<feTurbulence>` (radial/fractal noise) and `<feDisplacementMap>`.
2. On mouseenter, trigger a rapid GSAP timeline that blasts the displacement `scale` up to 100 and then immediately back to 0.
3. This creates a quick "shockwave" or "sonic boom" ripple distortion effect across the image upon interaction.
4. Provide complete self-contained HTML5, CSS3, and JS files.
```
