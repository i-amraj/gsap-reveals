# 🤖 AI Master Prompt: Ink Drop Liquid Reveal (`ink_drop_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Fashion Editorial style Ink Drop Liquid Reveal animation (`ink_drop_reveal`) using SVG Gooey Filters and GSAP 3.

Requirements:
1. Two stacked images (`nature1.jpeg` base, `nature2.jpeg` overlay).
2. The overlay image is masked by an element that uses an SVG Gooey filter (`<feGaussianBlur>` and `<feColorMatrix>`).
3. Inside the mask, an ink drop element starts at `scale: 0`.
4. On hover, GSAP animates the ink drop scale to `scale: 25`, organically expanding and revealing the overlay image like fluid ink spreading.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
