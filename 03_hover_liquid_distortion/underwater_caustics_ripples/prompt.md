# 🤖 AI Master Prompt: Underwater Caustics Ripples (`underwater_caustics_ripples`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Aquatic / Environmental style Underwater Caustics Ripples animation (`underwater_caustics_ripples`) using SVG Filters and GSAP 3.

Requirements:
1. Image filtered with `<feTurbulence>` (low `baseFrequency` to simulate slow underwater waves) and `<feDisplacementMap>`.
2. Add a `<feColorMatrix>` or overlay to give it a deep blue/cyan caustic light reflection vibe.
3. Continuously animate the `baseFrequency` or `<animate>` tag inside SVG to create a constant flowing wave.
4. On mouseenter, GSAP increases the displacement map `scale` and speeds up the baseFrequency transition to simulate intense underwater current.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
