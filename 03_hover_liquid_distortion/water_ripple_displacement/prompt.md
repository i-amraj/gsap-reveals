# 🤖 AI Master Prompt: Water Wave Liquid Ripple Displacement Hover (`water_ripple_displacement`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Awwwards / Luxury Agency style Water Wave Liquid Ripple Displacement Hover animation (`water_ripple_displacement`) using SVG Filters and GSAP 3.

Requirements:
1. Card container with image filtered via inline SVG `<feTurbulence>` and `<feDisplacementMap>`.
2. On mouseenter/mousemove, GSAP animates SVG `scale` from 0 to 45 and `baseFrequency` to simulate liquid water ripple flow across the image.
3. On mouseleave, GSAP smoothly settles SVG `scale` back to 0 with spring easing (`power2.out`).
4. Provide complete self-contained HTML5, CSS3, and JS files.
```
