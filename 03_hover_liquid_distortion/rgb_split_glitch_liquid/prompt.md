# 🤖 AI Master Prompt: RGB Split Chromatic Liquid Distortion Hover (`rgb_split_glitch_liquid`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Cyberpunk style RGB Split Chromatic Liquid Distortion Hover animation (`rgb_split_glitch_liquid`) using SVG Filters and GSAP 3.

Requirements:
1. Card container with image filtered via SVG `<feColorMatrix>` RGB channel splits and `<feDisplacementMap>`.
2. On hover, GSAP animates Red/Blue channel offsets (`dx` and `dy`) and liquid turbulence frequency.
3. On mouseleave, RGB offset smoothly snaps back to zero.
4. Provide complete self-contained HTML5, CSS3, and JS files.
```
