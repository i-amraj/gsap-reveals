# 🤖 AI Master Prompt: Fluid Glass Lens Refraction Distortion Hover (`fluid_glass_refraction`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Luxury Glassmorphism style Fluid Glass Lens Refraction Distortion Hover animation (`fluid_glass_refraction`) using GSAP 3.

Requirements:
1. Circular fluid glass lens element with `backdrop-filter: blur(12px) brightness(1.2)` and `transform: scale(1.3)`.
2. GSAP `quickTo` tracks mouse cursor position inside image container.
3. Lens expands smoothly on mouseenter (`scale: 1.0` to `1.0`) and contracts on mouseleave (`scale: 0`).
4. Provide complete self-contained HTML5, CSS3, and JS files.
```
