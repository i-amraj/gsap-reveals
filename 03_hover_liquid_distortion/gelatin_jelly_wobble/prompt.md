# 🤖 AI Master Prompt: Gelatin Jelly Wobble Hover (`gelatin_jelly_wobble`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Playful / Interactive style Gelatin Jelly Wobble Hover animation (`gelatin_jelly_wobble`) using SVG Filters and GSAP 3.

Requirements:
1. Card image filtered using an SVG `<feTurbulence>` and `<feDisplacementMap>`.
2. Base frequency for turbulence is set low (`0.01`).
3. On mouseenter, GSAP animates displacement `scale` up to 40 and quickly snaps it back to 0 using a strong spring ease (`elastic.out(1, 0.3)`).
4. This simulates a soft-body jelly wobble effect triggered by cursor interaction.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
