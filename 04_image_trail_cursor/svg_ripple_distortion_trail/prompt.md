# 🤖 AI Master Prompt: SVG Ripple Distortion Trail (`svg_ripple_distortion_trail`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an SVG Ripple Distortion Image Trail (`svg_ripple_distortion_trail`) using GSAP 3.

Requirements:
1. Add an invisible SVG `<filter>` with `<feTurbulence>` and `<feDisplacementMap>`.
2. Apply `filter: url(#rippleFilter)` to spawned images.
3. Track mouse coordinates and dynamically spawn images.
4. As the image spawns, use GSAP to animate the `baseFrequency` or `scale` attribute of the SVG filter so the image appears to ripple like a puddle of water before fading out.
5. Provide complete HTML5, CSS3, and JS files.
```
