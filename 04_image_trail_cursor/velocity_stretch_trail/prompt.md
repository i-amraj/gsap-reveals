# 🤖 AI Master Prompt: Velocity Stretch Trail (`velocity_stretch_trail`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Velocity Stretch Image Trail (`velocity_stretch_trail`) using GSAP 3.

Requirements:
1. Track mouse position to calculate velocity (speed and direction).
2. Spawn image elements sequentially along the mouse path.
3. Apply a dynamic `scaleY` (or skew) based on the mouse speed (faster = more stretched).
4. Apply a dynamic `rotation` based on the angle of mouse movement so the image stretches along the path of travel.
5. Animate opacity fading to 0 and remove the element from the DOM.
6. Provide complete self-contained HTML5, CSS3, and JS files.
```
