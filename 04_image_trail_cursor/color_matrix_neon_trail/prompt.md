# 🤖 AI Master Prompt: Color Matrix Neon Trail (`color_matrix_neon_trail`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Color Matrix Neon Image Trail (`color_matrix_neon_trail`) using GSAP 3.

Requirements:
1. Define 4-5 different SVG `<filter>` definitions in HTML, each using a `<feColorMatrix>` to apply extreme neon tints (Hot Pink, Cyberpunk Blue, Matrix Green, etc).
2. Track mouse coordinates to dynamically spawn images.
3. For each spawned image, randomly assign one of the predefined SVG filters via inline style (e.g., `filter: url(#neon-pink)`).
4. Animate pop in and fade out. 
5. The result is a vibrant, multi-colored cyberpunk trail.
6. Provide complete HTML5, CSS3, and JS files.
```
