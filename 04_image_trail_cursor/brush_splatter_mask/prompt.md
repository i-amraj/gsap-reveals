# 🤖 AI Master Prompt: Brush Splatter Mask (`brush_splatter_mask`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Brush Splatter Mask Image Trail (`brush_splatter_mask`) using GSAP 3 and CSS `mask-image`.

Requirements:
1. Track mouse coordinates and dynamically spawn images.
2. Instead of plain rectangles, apply a CSS `mask-image: radial-gradient(circle, black, transparent)` or a custom SVG paint splatter data-uri to the spawned element to make it look like an ink/paint splatter.
3. Animate the image popping in (scale from 0 to 1).
4. Fade it out to opacity 0 after a short delay and remove it.
5. Provide complete HTML5, CSS3, and JS files.
```
