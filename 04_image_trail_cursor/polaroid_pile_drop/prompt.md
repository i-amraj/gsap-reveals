# 🤖 AI Master Prompt: Polaroid Pile Drop (`polaroid_pile_drop`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Polaroid Pile Drop Trail (`polaroid_pile_drop`) using GSAP 3.

Requirements:
1. Track mouse coordinates and dynamically spawn images styled as polaroids.
2. Animate the image popping in at the cursor.
3. Instead of fading out, animate the image falling to the bottom of the screen (or a fixed ground line).
4. Do NOT remove the image from the DOM. Let it stay permanently at the bottom.
5. Add random slight rotation to the final resting state so they pile up messily.
6. Cap the max DOM nodes (e.g., if array length > 40, remove oldest) to prevent lag.
7. Provide complete HTML5, CSS3, and JS files.
```
