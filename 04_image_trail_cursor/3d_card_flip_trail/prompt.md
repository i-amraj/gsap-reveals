# 🤖 AI Master Prompt: 3D Card Flip Trail (`3d_card_flip_trail`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a 3D Card Flip Image Trail (`3d_card_flip_trail`) using GSAP 3.

Requirements:
1. Ensure the parent container has `perspective: 1000px` to enable 3D space.
2. Track mouse coordinates and dynamically spawn images.
3. Spawn the image at `rotationY: -180` and `opacity: 0`.
4. Animate it flipping into place (`rotationY: 0`) and popping up (`scale: 1`).
5. After a short delay, flip it out again (`rotationY: 180`) while fading to 0 and remove it.
6. Provide complete HTML5, CSS3, and JS files.
```
