# 🤖 AI Master Prompt: Spiral Vortex Suck (`spiral_vortex_suck`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Spiral Vortex Suck Image Trail (`spiral_vortex_suck`) using GSAP 3.

Requirements:
1. Define a "black hole" coordinate at the exact center of the screen `(window.innerWidth/2, window.innerHeight/2)`.
2. Track mouse coordinates and dynamically spawn images.
3. As the image pops in at the mouse location, immediately animate it moving towards the center black hole coordinate.
4. As it moves towards the center, animate `rotation` (e.g. `+360`), `scale` (down to `0`), and `opacity` (to `0`) to simulate it being sucked into a vortex.
5. Apply an ease like `power3.in` to make it accelerate as it approaches the center.
6. Provide complete HTML5, CSS3, and JS files.
```
