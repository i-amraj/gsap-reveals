# 🤖 AI Master Prompt: Tunnel Vision Flythrough (`tunnel_vision_flythrough`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Tunnel Vision Flythrough Trail (`tunnel_vision_flythrough`) using GSAP 3.

Requirements:
1. Track mouse coordinates and dynamically spawn images.
2. Spawn images at `scale: 0.1` and `opacity: 0`.
3. Animate the image popping in, but immediately scale it UP massively (e.g. `scale: 15` or higher) to simulate the camera flying "through" the image.
4. Animate `opacity` to 1, then back to 0 as it gets too large (clipping the camera).
5. Add random rotation during the scale up to make the tunnel dynamic.
6. Provide complete HTML5, CSS3, and JS files.
```
