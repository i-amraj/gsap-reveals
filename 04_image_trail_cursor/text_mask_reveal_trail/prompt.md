# 🤖 AI Master Prompt: Text Mask Reveal Trail (`text_mask_reveal_trail`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Text Mask Reveal Trail (`text_mask_reveal_trail`) using GSAP 3 and CSS `mix-blend-mode`.

Requirements:
1. Create a full-screen white background with a massive, bold black typography element centered (e.g., `h1` saying "REVEAL").
2. Track mouse coordinates to dynamically spawn images (the trail).
3. Ensure the trail images have a lower `z-index` than the text OR use CSS `mix-blend-mode: screen/lighten` on the text so that the spawned images are ONLY visible *inside* the bold letters. (Using a white background with black text and `mix-blend-mode: screen` on the text layer makes the images show through the black text).
4. Animate images popping in and fading out.
5. Provide complete HTML5, CSS3, and JS files.
```
