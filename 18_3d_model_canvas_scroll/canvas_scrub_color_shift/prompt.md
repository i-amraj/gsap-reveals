# Canvas Scrub Color Shift
Animates the CSS `filter: hue-rotate(...)` property of the canvas in parallel with the frame rendering. This is a very cheap way to change the color of a rendered 3D product sequence without needing to load 3 separate color image sequences.
