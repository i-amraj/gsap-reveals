# YoYo Canvas Scrub
Instead of a linear start-to-finish sequence, the GSAP timeline is programmed to scrub forward through the sequence for the first half of the scroll, and then automatically play backward to frame `0` for the second half.
