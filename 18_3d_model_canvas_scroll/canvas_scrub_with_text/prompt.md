# Canvas Scrub with Text
Builds on the basic scrub by creating a master GSAP `timeline`. The `currentFrame` animation spans the entire timeline, while text elements are placed at specific timestamps (`+=0`, `1`, `4`) using the position parameter to fade in and out exactly as specific frames are being drawn.
