# Reverse Canvas Scrub
A simple conceptual twist: initializing the `currentFrame` object at the maximum frame count and animating it `to` `0`. This makes the sequence play in reverse as the user scrolls downwards.
