# Canvas Scrub with Progress Bar
Ties a CSS width-based progress bar perfectly to the `onUpdate: (self) => self.progress` hook inside the ScrollTrigger controlling the canvas, providing users a clear visual indicator of the 3D rotation duration.
