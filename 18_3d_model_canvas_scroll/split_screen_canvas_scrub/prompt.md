# Split Screen Canvas Scrub
Uses CSS `position: sticky` on a 50% width container to hold the canvas, while the other 50% contains tall scrolling text blocks. The `ScrollTrigger` scrubs the canvas based on the height of the scrolling text section.
