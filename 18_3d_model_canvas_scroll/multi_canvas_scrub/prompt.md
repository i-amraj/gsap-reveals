# Multi Canvas Scrub
A single GSAP ScrollTrigger timeline controls the `currentFrame` object, but the `onUpdate` render function draws to multiple distinct `<canvas>` elements simultaneously, allowing for side-by-side product comparisons.
