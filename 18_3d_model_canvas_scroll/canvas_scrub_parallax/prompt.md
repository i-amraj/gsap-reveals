# Canvas Scrub Parallax
Instead of pinning, the section scrolls naturally. A single ScrollTrigger timeline scrubs the canvas frames while simultaneously animating the `y` positions of the background, the canvas itself, and a foreground element at different speeds to create depth.
