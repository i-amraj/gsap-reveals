# Canvas Scrub with SVG Mask
The `<canvas>` element is wrapped in a container that has a CSS `clip-path: url(#my-clip)` applied. This allows the 3D rotating product to appear constrained inside a complex custom vector shape.
