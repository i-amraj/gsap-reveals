# Canvas Scrub with Audio
Ties the ScrollTrigger's `self.progress` variable to the Web Audio API (or HTML5 Audio volume), allowing the visual scrubbing to perfectly sync with an auditory cue (e.g., a hum that gets louder as the model rotates to face the camera).
