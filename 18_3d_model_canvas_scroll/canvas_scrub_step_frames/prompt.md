# Canvas Scrub Step Frames
Uses GSAP's `steps(n)` ease function instead of `none`. This forces the scrub to "snap" to specific frames without smooth interpolation, creating a stop-motion or mechanical click-wheel effect.
