# Canvas Scrub 3D Tilt
Uses CSS `perspective` on a container and applies GSAP `rotationX`, `rotationY`, and `scale` to the `<canvas>` DOM element itself, while the internal canvas `ctx` rendering scrubs simultaneously. This creates a "fly-in" 3D effect of the object while it rotates.
