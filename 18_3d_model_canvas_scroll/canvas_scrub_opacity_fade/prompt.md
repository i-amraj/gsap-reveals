# Canvas Scrub Opacity Fade
A timeline precisely controls the DOM canvas opacity alongside the sequence render. The canvas gracefully fades in at the start of the pin, rotates, and fades out cleanly before unpinning.
