# Horizontal Canvas Scrub
Uses GSAP's advanced `containerAnimation` feature to scrub the canvas frames *only* while the canvas panel is scrolling horizontally across the viewport, rather than tying it to vertical scroll position directly.
