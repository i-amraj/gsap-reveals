# Canvas Blur Scrub
Animates the CSS `filter: blur(...)` property of the canvas DOM element simultaneously alongside the frame rendering, causing the rotating model to come into sharp focus halfway through the scroll before blurring out again.
