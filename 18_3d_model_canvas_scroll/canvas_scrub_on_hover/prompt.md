# Canvas Scrub on Hover
Replaces ScrollTrigger with a native `mousemove` event listener on a container. The horizontal mouse position is mapped to the `currentFrame`, allowing users to manually rotate/scrub the model by moving their cursor left and right.
