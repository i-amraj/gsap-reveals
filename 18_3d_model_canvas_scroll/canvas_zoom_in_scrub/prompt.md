# Canvas Zoom In Scrub
Combines scrubbing the canvas `currentFrame` object with a simultaneous CSS `scale` animation applied directly to the `<canvas>` DOM element, creating a dramatic push-in effect while the object rotates.
