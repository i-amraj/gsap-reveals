# Basic Canvas Scrub
The fundamental Apple-style effect: pinning a full-screen canvas and using ScrollTrigger `scrub` to animate a numeric `frame` variable. On every update, the canvas redraws the corresponding frame (in this demo, a procedurally generated 3D rotation).
