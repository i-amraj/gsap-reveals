# 🤖 AI Master Prompt: Overlapping Strip Venetian Parallax (`overlapping_slat_venetian_parallax`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Architectural Editorial style Overlapping Strip Venetian Parallax animation (`overlapping_slat_venetian_parallax`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Container divided into 5 vertical strip elements (`display: flex`, width 20% each).
2. Each strip contains a slice of the same image aligned via `background-position` or offset `img`.
3. GSAP ScrollTrigger timeline animates each strip vertically with staggered parallax rates (from `yPercent: 0` to `-15%`, `-30%`, `-45%`, `-30%`, `-15%`).
4. ScrollTrigger configured with `scrub: 1`.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
