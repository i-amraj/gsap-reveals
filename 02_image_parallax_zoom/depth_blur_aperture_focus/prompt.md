# 🤖 AI Master Prompt: Depth Blur Aperture Focus Parallax (`depth_blur_aperture_focus`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Cinematic Photography style Depth Blur Aperture Focus Parallax animation (`depth_blur_aperture_focus`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Pinned viewport stage containing image card.
2. Image starts at `scale: 1.35` with Gaussian blur `filter: blur(28px)`.
3. GSAP ScrollTrigger timeline animates image scale down to `scale: 1.0` and clears blur to `filter: blur(0px)`.
4. Foreground text overlay reveals with staggered fade-in as focus sharpens.
5. ScrollTrigger configured with `pin: true`, `start: 'top top'`, `end: '+=1800'`, and `scrub: 1`.
6. Provide complete self-contained HTML5, CSS3, and JS files.
```
