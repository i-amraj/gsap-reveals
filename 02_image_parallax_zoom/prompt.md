# 🤖 AI Master Prompt: Image Parallax Depth & Scale Zoom (`02_image_parallax_zoom`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Apple-style Image Parallax Depth & Scale Zoom scroll animation (`02_image_parallax_zoom`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Multi-card layout where each image is wrapped inside a container with `overflow: hidden`.
2. Image initial CSS height set to 120% to allow smooth vertical translation without showing empty borders.
3. GSAP ScrollTrigger animating image `scale` from 1.0 to 1.35 and `yPercent` from 0 to -20%.
4. Smooth `scrub: 1` scroll binding for realistic 3D depth camera movement.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
