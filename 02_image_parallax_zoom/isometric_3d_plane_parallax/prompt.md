# 🤖 AI Master Prompt: Isometric 3D Plane Parallax (`isometric_3d_plane_parallax`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Web3 / Fintech style Isometric 3D Plane Parallax scroll animation (`isometric_3d_plane_parallax`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Outer wrapper container set with `perspective: 1200px` and isometric tilt (`rotateX: 48deg`, `rotateZ: -24deg`).
2. Stacked cards glide along the isometric plane on scroll.
3. Card images elevate in 3D Z-height (`translateZ: 180px`) and scale up (`scale: 1.15`).
4. ScrollTrigger configured with `pin: true`, `start: 'top top'`, `end: '+=2000'`, and `scrub: 1`.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
