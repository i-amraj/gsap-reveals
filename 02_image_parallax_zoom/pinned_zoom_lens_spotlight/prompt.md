# 🤖 AI Master Prompt: Scroll Magnifying Zoom Lens (`pinned_zoom_lens_spotlight`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Luxury E-Commerce / Watch Inspector style Scroll Magnifying Zoom Lens animation (`pinned_zoom_lens_spotlight`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Base image pinned inside a full-viewport section.
2. Circular lens element (`width: 250px`, `height: 250px`, `border-radius: 50%`) with `overflow: hidden` and glass shadow.
3. ScrollTrigger timeline animates the lens position across key coordinates (left: 20% -> 80%, top: 30% -> 70%).
4. Inner image inside lens is scaled 2.5x and counter-shifted in real time to match background position accurately.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
