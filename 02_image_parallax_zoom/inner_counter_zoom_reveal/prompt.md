# 🤖 AI Master Prompt: Card Expand & Inner Counter-Zoom Reveal (`inner_counter_zoom_reveal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Nike/Porsche style Card Expand & Inner Counter-Zoom scroll animation (`inner_counter_zoom_reveal`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Outer card container starts at width 45vw and expands to 100vw full screen on scroll (`border-radius: 40px` to `0px`).
2. Inner image counter-scales from `scale: 1.6` down to `scale: 1.0` simultaneously to keep visual subject center-pinned.
3. ScrollTrigger configured with `pin: true`, `start: 'top top'`, `end: '+=1500'`, and `scrub: 1`.
4. Provide complete self-contained HTML5, CSS3, and JS files.
```
