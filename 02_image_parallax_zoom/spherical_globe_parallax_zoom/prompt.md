# 🤖 AI Master Prompt: Fisheye Globe Un-Distort Parallax (`spherical_globe_parallax_zoom`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Camera Tech / Product Unboxing style Fisheye Globe Un-Distort Parallax animation (`spherical_globe_parallax_zoom`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Card starts styled as a 3D spherical lens sphere: `border-radius: 50%`, `width: 350px`, `height: 350px`.
2. Inner image starts zoomed at `scale: 1.85`.
3. GSAP ScrollTrigger timeline animates card to full rectangle `width: 70vw`, `height: 65vh`, `border-radius: 28px`.
4. Inner image counter-zooms to `scale: 1.0` simultaneously.
5. ScrollTrigger configured with `pin: true`, `start: 'top top'`, `end: '+=1800'`, and `scrub: 1`.
6. Provide complete self-contained HTML5, CSS3, and JS files.
```
