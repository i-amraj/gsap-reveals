# 🤖 AI Master Prompt: Matrix Counter-Grid Parallax (`parallax_image_grid_distortion`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Architecture / Art Portfolio style Matrix Counter-Grid Parallax animation (`parallax_image_grid_distortion`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Three-column grid layout containing stacked cards with `overflow: hidden`.
2. Odd columns (Column 1 & 3) scroll UP (`yPercent: -25%`).
3. Even columns (Column 2) scroll DOWN (`yPercent: 25%`) simultaneously.
4. Smooth `scrub: 1` scroll binding for counter-grid distortion physics.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
