# 🤖 AI Master Prompt: 3D Prism Kaleidoscope Zoom (`prism_kaleidoscope_3d_zoom`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Cyberpunk / NFT style 3D Prism Kaleidoscope Zoom animation (`prism_kaleidoscope_3d_zoom`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Container set with `perspective: 1200px` and `transform-style: preserve-3d`.
2. Image split into 4 triangular clip-path facets (Top-Left, Top-Right, Bottom-Left, Bottom-Right).
3. GSAP ScrollTrigger timeline unfolds facets in 3D space (`rotateX: 45deg`, `rotateY: 45deg`, `z: 200px`) and zooms out to `scale: 1.0` flat composite picture.
4. ScrollTrigger configured with `pin: true`, `start: 'top top'`, `end: '+=2000'`, and `scrub: 1`.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
