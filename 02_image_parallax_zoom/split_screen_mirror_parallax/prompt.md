# 🤖 AI Master Prompt: Split Screen Mirror Parallax (`split_screen_mirror_parallax`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Editorial Magazine style Split Screen Mirror Parallax scroll animation (`split_screen_mirror_parallax`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Two-column split viewport section (50% width each column) with `overflow: hidden`.
2. Left column image animates UP (`yPercent: -35%`), while Right column image animates DOWN (`yPercent: 35%`) simultaneously.
3. Smooth `scrub: 1` scroll binding for mirrored counter-motion.
4. Provide complete self-contained HTML5, CSS3, and JS files.
```
