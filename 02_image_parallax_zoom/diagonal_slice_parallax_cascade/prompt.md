# 🤖 AI Master Prompt: Diagonal Slice Parallax Cascade (`diagonal_slice_parallax_cascade`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Sports / Action Editorial style Diagonal Slice Parallax Cascade animation (`diagonal_slice_parallax_cascade`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Container divided into 4 diagonal polygon strips (`clip-path: polygon(...)`).
2. GSAP ScrollTrigger timeline animates each strip along a 45-degree vector (`x` and `y` offsets) at staggered parallax rates.
3. As scroll completes, strips snap perfectly together into a single unified image composite.
4. ScrollTrigger configured with `scrub: 1`.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
