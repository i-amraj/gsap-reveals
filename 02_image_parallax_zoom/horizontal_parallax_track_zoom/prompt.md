# 🤖 AI Master Prompt: Horizontal Track Parallax Zoom (`horizontal_parallax_track_zoom`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Awwwards-style Horizontal Track Parallax Zoom scroll animation (`horizontal_parallax_track_zoom`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Pinned viewport section containing a wide flex track (`display: flex`, width 300vw).
2. Vertical scroll moves the track horizontally right-to-left (`xPercent: -66.6%`).
3. Images inside each card slide in the opposite horizontal direction (`xPercent: 25%`) and scale up (`scale: 1.35`).
4. ScrollTrigger configured with `pin: true`, `scrub: 1`, `start: 'top top'`, and `end: '+=2000'`.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
