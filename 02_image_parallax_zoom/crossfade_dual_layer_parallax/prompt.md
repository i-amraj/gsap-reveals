# 🤖 AI Master Prompt: Dual-Asset Counter Crossfade Parallax (`crossfade_dual_layer_parallax`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Editorial Storytelling / Before-After style Dual-Asset Counter Crossfade Parallax animation (`crossfade_dual_layer_parallax`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Pinned viewport section containing a stacked card with two layered images (`nature1.jpeg` base layer, `nature2.jpeg` top layer).
2. Base layer moves UP (`yPercent: -20%`), Top layer moves DOWN (`yPercent: 20%`).
3. Top layer opacity crossfades from `opacity: 0` to `opacity: 1` as scroll progresses.
4. ScrollTrigger configured with `pin: true`, `start: 'top top'`, `end: '+=1800'`, and `scrub: 1`.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
