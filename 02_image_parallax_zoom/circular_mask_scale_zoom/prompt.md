# 🤖 AI Master Prompt: Circle Mask Expand & Parallax Zoom (`circular_mask_scale_zoom`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Rolex/Porsche style Circle Mask Expand & Parallax Zoom scroll animation (`circular_mask_scale_zoom`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Pinned viewport section containing a top mask layer styled with `clip-path: circle(12% at 50% 50%)`.
2. ScrollTrigger timeline expands circle mask to `clip-path: circle(100% at 50% 50%)`.
3. Simultaneously, inner image counter-zooms out from `scale: 2.2` down to `scale: 1.0`.
4. ScrollTrigger configured with `pin: true`, `start: 'top top'`, `end: '+=1800'`, and `scrub: 1`.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
