# 🤖 AI Master Prompt: Mouse 3D Tilt + Scroll Parallax (`mouse_tilt_3d_parallax`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Gaming/NFT style Mouse 3D Tilt + Scroll Parallax card animation (`mouse_tilt_3d_parallax`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Card wrapper set with CSS `perspective: 1000px` and `transform-style: preserve-3d`.
2. ScrollTrigger scrub moves image Y position vertically (`yPercent: -20`).
3. Mouse movement over card calculates tilt angles (`rotateX` up to 15deg, `rotateY` up to 15deg) with smooth GSAP quickTo interpolation.
4. Glass glare reflection layer moves dynamically based on cursor X/Y coordinates.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
