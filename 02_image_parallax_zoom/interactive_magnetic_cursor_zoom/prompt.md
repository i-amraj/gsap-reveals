# 🤖 AI Master Prompt: Interactive Magnetic Cursor Zoom (`interactive_magnetic_cursor_zoom`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Interactive Design Agency style Magnetic Cursor Zoom animation (`interactive_magnetic_cursor_zoom`) using GSAP 3.

Requirements:
1. Card element reacts to mouse cursor proximity inside container bounds.
2. GSAP `quickTo` smooth physics interpolates card position (`x` and `y` magnetic pull up to 40px).
3. Inner image scales to `scale: 1.25` on cursor hover with smooth spring easing.
4. Card resets smoothly to origin (`x: 0, y: 0, scale: 1.0`) on mouseleave.
5. Provide complete self-contained HTML5, CSS3, and JS files.
```
