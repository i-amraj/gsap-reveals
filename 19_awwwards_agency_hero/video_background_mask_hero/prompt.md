# Video Background Mask Hero
Uses a CSS `mix-blend-mode: screen` trick with a black text layer over a white background placed on top of a video. As the user scrolls, ScrollTrigger massively scales up the typography until the user effectively "flies through" the cut-out text into the video behind it.
