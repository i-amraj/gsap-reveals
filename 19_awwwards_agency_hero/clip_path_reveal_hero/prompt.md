# Clip Path Reveal Hero
The background image starts collapsed into a single point in the center via `clip-path: polygon(50% 50%...)`. On load, GSAP dramatically expands the clip-path to fill the screen, followed by a bounce-in typography reveal.
