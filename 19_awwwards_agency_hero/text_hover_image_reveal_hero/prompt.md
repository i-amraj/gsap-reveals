# Text Hover Image Reveal Hero
A portfolio staple. A massive list of text links is centered in the hero. When hovering over a text link, an image associated with that link reveals itself and smoothly trails the user's cursor using GSAP `quickTo()`. The text uses `mix-blend-mode` to stay visible over the image.
