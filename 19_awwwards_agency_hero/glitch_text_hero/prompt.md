# Glitch Text Hero
Uses absolute positioned layers for text (white, red, blue) combined with `mix-blend-mode: screen` and GSAP's `CustomEase` to rapidly shift the RGB layers independently, simulating an intense cyberpunk digital glitch on load and hover.
