# Circular Text Marquee Hero
Uses an SVG `<textPath>` to wrap text in a perfect circle creating a "badge". The badge slowly rotates infinitely via CSS keyframes, but GSAP ScrollTrigger adds an *additional* rotation layer on scroll, making it spin rapidly as the user scrolls down.
