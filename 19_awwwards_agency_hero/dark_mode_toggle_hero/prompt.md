# Dark Mode Toggle Hero
A sophisticated theme toggle. Instead of just swapping variables instantly, GSAP expands a massive colored circle from the toggle button's exact coordinates. The content text uses `mix-blend-mode: difference` so it smoothly inverts its color mid-animation as the expanding circle passes underneath it.
