# Cursor Follower Hero
Implements the extremely popular "magnetic button" and custom GSAP `quickTo()` cursor follower. When hovering over the CTA, the button physically pulls toward the cursor while the text inside moves at a different rate (parallax), and the custom cursor circle expands.
