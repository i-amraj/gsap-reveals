# Fluid Morphing Hero
Features an organic SVG blob in the background that scales and rotates infinitely to simulate fluid morphing. It also tracks the mouse cursor via `gsap.quickTo()` for a subtle parallax, acting as a soft, ambient backdrop for elegant typography.
