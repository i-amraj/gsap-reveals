# Typographic Grid Hero
Uses CSS Grid with a `gap` to create 1px borders. GSAP staggers the reveal of the grid cells, then staggers the internal elements (massive text, an image scaling down, and a descriptive paragraph), creating a very rigid, editorial, Swiss-design aesthetic.
