# Classic Split Text Hero
The quintessential Awwwards agency hero. Features massive Oswald typography masked within lines, animating upwards (`y: 110%` to `y: 0`), combined with an image that reveals via `clip-path: inset(...)` while scaling down.
