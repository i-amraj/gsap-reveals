# Parallax Image Hero
A multi-layered mouse-parallax hero. The background, text, and foreground layers are oversized (`110%`) and bound to GSAP `quickTo()` functions mapped to the mouse position. Each layer moves at a different depth multiplier, creating a stunning 3D illusion.
