# 🤖 AI Master Prompt: Cursor Spotlight Tilt (`cursor_spotlight_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Cursor Spotlight Tilt Hover effect (`cursor_spotlight_tilt`) using GSAP 3.

Requirements:
1. Create a dark 3D tilt card.
2. Inside the card, add an absolute div covering the card for the spotlight effect (`.spotlight`).
3. Set the `.spotlight` background to a radial-gradient (e.g., white to transparent) and `mix-blend-mode: overlay` or `soft-light`.
4. Hide the card's inner content by default (low opacity), and make the spotlight illuminate it.
5. As the user tilts the card, use GSAP to update the `background-position` or `x/y` coordinates of the radial gradient to perfectly follow the mouse, acting like a flashlight on a 3D surface.
6. Provide complete HTML5, CSS3, and JS files.
```
