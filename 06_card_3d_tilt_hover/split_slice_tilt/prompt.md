# 🤖 AI Master Prompt: Split Slice Tilt (`split_slice_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Split Slice Tilt Hover effect (`split_slice_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card container.
2. Inside the card wrapper, create 3 separate `.slice` divs side-by-side (using flexbox), each taking up 33.3% width.
3. The slices should piece together to look like one complete image or card.
4. On `mousemove` over the wrapper, tilt all three slices together.
5. However, give each slice a different `translateZ` depth when hovered, so they physically separate in 3D space like a sliced deck of cards separating mid-air.
6. Provide complete HTML5, CSS3, and JS files.
```
