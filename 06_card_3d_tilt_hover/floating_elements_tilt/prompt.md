# 🤖 AI Master Prompt: Floating Elements Tilt (`floating_elements_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Floating Elements Tilt Hover effect (`floating_elements_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card as usual with GSAP mapping mouse position to X/Y rotation.
2. Inside the card, add elements (an image, a title, a button) that have CSS `transform: translateZ(...)`.
3. Because the parent card has `transform-style: preserve-3d` and is being rotated, the child elements with `translateZ` will visually pop out of the card in true 3D space, creating a strong parallax depth effect.
4. Provide complete HTML5, CSS3, and JS files.
```
