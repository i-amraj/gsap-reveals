# 🤖 AI Master Prompt: Glare Lighting Tilt (`glare_lighting_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Glare Lighting Tilt Hover effect (`glare_lighting_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card (similar to `basic_3d_tilt`).
2. Inside the card, add an absolute div for `.glare` with a radial gradient (white to transparent).
3. Set the `.glare` pointer-events to none and use `mix-blend-mode: overlay` or `soft-light`.
4. As the mouse moves over the card, update the tilt (rotation) AND simultaneously use GSAP to animate the position of the `.glare` element to follow the mouse.
5. This simulates realistic dynamic lighting hitting a shiny surface.
6. Provide complete HTML5, CSS3, and JS files.
```
