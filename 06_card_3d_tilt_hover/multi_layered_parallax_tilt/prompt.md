# 🤖 AI Master Prompt: Multi-Layered Parallax Tilt (`multi_layered_parallax_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Multi-Layered Parallax Tilt Hover effect (`multi_layered_parallax_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card where the card acts purely as a bounding box (`perspective: 1500px`, `transform-style: preserve-3d`).
2. Inside the card, create 4-5 transparent layers (`absolute` positioned) that stack on top of each other.
3. Each layer should have a progressively higher `translateZ` value (e.g., `translateZ(20px)`, `translateZ(40px)`, etc.).
4. When the user hovers and tilts the card, the layers physically separate in 3D space, creating an intense "diorama" or multi-plane camera effect.
5. Provide complete HTML5, CSS3, and JS files.
```
