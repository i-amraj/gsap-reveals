# 🤖 AI Master Prompt: Holographic Foil Tilt (`holographic_foil_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Holographic Foil Tilt Hover effect (`holographic_foil_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card (rare Pokémon trading card style).
2. Inside the card, add an absolute `.foil` div covering it entirely.
3. The `.foil` should use a complex, multi-color linear-gradient (rainbow colors) to simulate a holographic reflection. Set `mix-blend-mode: color-dodge` or similar.
4. As the mouse moves, update the card's tilt (GSAP `rotationX`, `rotationY`).
5. **Crucially:** Map the mouse position to update the `background-position` or `opacity` of the `.foil` gradient using GSAP, creating the illusion of holographic light shifting across the card.
6. Provide complete HTML5, CSS3, and JS files.
```
