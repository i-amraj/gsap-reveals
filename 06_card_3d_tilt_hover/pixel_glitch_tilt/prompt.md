# 🤖 AI Master Prompt: Pixel Glitch Tilt (`pixel_glitch_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Pixel Glitch Tilt effect (`pixel_glitch_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card.
2. In JS, calculate the "velocity" of the mouse movement (distance covered between frames).
3. If the user tilts the card too fast (velocity exceeds a threshold), add a `.glitching` class to the card.
4. The `.glitching` class should use CSS keyframes and CSS filters (e.g., `hue-rotate`, `contrast`, `skew`, `clip-path` slices) to simulate a digital screen breaking/glitching.
5. When the user stops moving the mouse fast, remove the `.glitching` class.
6. Provide complete HTML5, CSS3, and JS files.
```
