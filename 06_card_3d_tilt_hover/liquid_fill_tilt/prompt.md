# 🤖 AI Master Prompt: Liquid Fill Tilt (`liquid_fill_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Liquid Fill Tilt Hover effect (`liquid_fill_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card.
2. Inside the card, add a `.liquid` container at the bottom with a CSS wave animation (using animated border-radius squares spinning).
3. The card tilts in 3D using standard GSAP `rotationX` and `rotationY`.
4. To simulate fluid physics, animate the 2D `rotation` (Z-axis) of the `.liquid` container in the OPPOSITE direction of the card's `rotationY` tilt. 
5. As the user tilts the card left/right, the water level remains horizontal relative to the screen, sloshing dynamically.
6. Provide complete HTML5, CSS3, and JS files.
```
