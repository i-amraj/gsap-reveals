# 🤖 AI Master Prompt: Glass Distortion Tilt (`glass_distortion_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Glass Distortion Tilt Hover effect (`glass_distortion_tilt`) using GSAP 3.

Requirements:
1. Set a colorful/detailed background image on the `body` or a main wrapper container.
2. Create a 3D tilt card in the center.
3. The card itself should have a translucent background (e.g., `rgba(255, 255, 255, 0.1)`) and a strong CSS `backdrop-filter: blur(20px)`.
4. As the user tilts the card using GSAP, the blur will visually distort different parts of the background image behind it dynamically.
5. Add a subtle white border `rgba(255, 255, 255, 0.3)` to act as the glass edge.
6. Provide complete HTML5, CSS3, and JS files.
```
