# 🤖 AI Master Prompt: Neon Edge Glow Tilt (`neon_edge_glow_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Neon Edge Glow Tilt Hover effect (`neon_edge_glow_tilt`) using GSAP 3.

Requirements:
1. Create a dark 3D tilt card.
2. Inside the card, create a dynamic neon rim light effect.
3. This is best achieved by updating an `inset` CSS `box-shadow` using GSAP during the `mousemove` event.
4. As the card tilts left, the left inner edge should glow intensely (e.g., cyan/magenta). As it tilts right, the right edge glows.
5. Provide complete HTML5, CSS3, and JS files.
```
