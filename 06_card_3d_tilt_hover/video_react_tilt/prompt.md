# 🤖 AI Master Prompt: Video React Tilt (`video_react_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Video React Tilt Hover effect (`video_react_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card.
2. Inside the card, add a `<video>` element set to loop, autoplay, and muted as the background.
3. The video should have `object-fit: cover` and slightly overhang the card bounds (e.g., scale 1.1).
4. When the user tilts the card, map the mouse position to physically translate (`x`, `y`) the video slightly in the OPPOSITE direction of the tilt to create an intense internal parallax effect, separate from the 3D rotation of the card itself.
5. Provide complete HTML5, CSS3, and JS files.
```
