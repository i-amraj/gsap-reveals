# 🤖 AI Master Prompt: Shadow Cast Tilt (`shadow_cast_tilt`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Shadow Cast Tilt Hover effect (`shadow_cast_tilt`) using GSAP 3.

Requirements:
1. Create a 3D tilt card.
2. Directly underneath the card in the DOM (but outside the card's 3D rotation), create a `.floor-shadow` element.
3. This shadow should look like a dark, blurry oval on the ground.
4. As the user tilts the card, map the mouse X and Y to translate (`x`, `y`) and scale (`scale`) the shadow in the OPPOSITE direction. 
5. If the card tilts up and left, the shadow should move down and right, simulating a fixed light source directly above the card.
6. Provide complete HTML5, CSS3, and JS files.
```
