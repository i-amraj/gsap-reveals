# 🤖 AI Master Prompt: Magnetic Tilt Hover (`magnetic_tilt_hover`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Magnetic Tilt Hover effect (`magnetic_tilt_hover`) using GSAP 3.

Requirements:
1. Create a 3D tilt card (similar to `basic_3d_tilt`).
2. In addition to rotating the card based on mouse position (tilt), ALSO animate the `x` and `y` translation of the card so it gets physically "pulled" towards the cursor slightly (magnetic effect).
3. The translation should be subtle (e.g., max 20px on X and Y).
4. On `mouseleave`, reset both the rotation and translation back to 0 with an elastic bounce.
5. Provide complete HTML5, CSS3, and JS files.
```
