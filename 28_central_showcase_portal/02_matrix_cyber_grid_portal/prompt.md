# AI Prompt: Cyber Matrix Command Palette Portal

```markdown
Build a cyberpunk hacker-terminal showcase portal with command palette search and CRT scanline styling using GSAP 3.

Key Requirements:
1. Terminal Aesthetics:
   - CRT green phosphor overlay (#00ff66) with subtle scanlines and monospace typography.
   - Blinking cursor prompt and shortcut badge for keyboard shortcut Ctrl+K.
2. Dynamic Search & Filtering:
   - Live query parsing instantly re-rendering matrix grid cards with GSAP stagger animation.
   - Ctrl+K shortcut listens globally to focus and select the search input.
3. Node Inspection Modal:
   - Clicking any cyber card opens a glowing modal window displaying the raw AI prompt and launch link.
```
