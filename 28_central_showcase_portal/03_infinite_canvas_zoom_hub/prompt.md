# AI Prompt: Figma-Style Infinite Spatial Canvas Hub

```markdown
Build an infinite 2D spatial canvas showcase portal with pan and zoom capabilities using GSAP Draggable.

Key Requirements:
1. Infinite 2D World Navigation:
   - Make the canvas container draggable across both X and Y axes using GSAP Draggable.
   - Mouse wheel listener adjusts canvas scale (0.4x to 2.2x) zooming towards cursor coordinates.
2. Spatial HUD & Clustered Cards:
   - Modern HUD showing zoom percentage with Zoom In, Zoom Out, and Reset buttons.
   - Category clusters scattered in 2D space with glassmorphic cards and dot grid background.
```
