# MotionPath Logo Orbit
Showcases `MotionPathPlugin` configuring multiple elements to orbit endlessly around an SVG circle path. By utilizing `start` and `end` mapping on the `motionPath` object, the JS/CSS/SVG/Canvas satellites are distributed perfectly at 25% intervals across the circular track. Hovering over any element pauses the `gsap.globalTimeline`.
