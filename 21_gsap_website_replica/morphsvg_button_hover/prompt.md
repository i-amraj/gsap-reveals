# MorphSVG Button Hover
Instead of traditional CSS border-radius manipulation, this button uses an inline SVG as its background. On hover, GSAP manipulates the path data via the `attr: { d: "..." }` tween to morph the clean pill shape into a jagged, organic, elastic blob, showcasing the power of SVG path animation for micro-interactions.
