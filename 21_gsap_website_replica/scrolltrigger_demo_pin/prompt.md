# ScrollTrigger Demo Pin
Recreates the classic "How ScrollTrigger Works" explainer on the GSAP site. The left column contains a green box that pins precisely at `center center`. As the right column scrolls, `endTrigger` unpins the box at the exact moment the final text block hits center, perfectly linking scrub animations to specific structural DOM markers.
