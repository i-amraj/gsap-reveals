# FLIP Plugin Grid to List
Demonstrates the official GSAP FLIP plugin (`Flip.getState()` and `Flip.from()`). We have a parent container that completely alters its CSS flexbox structure (`flex-direction`, child widths) upon a button click. GSAP FLIP handles the complex First, Last, Invert, Play math to smoothly interpolate all elements to their new fluid layout bounds.
