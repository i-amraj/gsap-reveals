# Custom Ease Graph
A replica of the GSAP CustomEase visualizer tool. A timeline controls three concurrent tweens over 2 seconds: the SVG path drawing in linearly, a ball moving on the X-axis linearly, and the same ball moving on the Y-axis utilizing `CustomEase.create()` path logic. This perfectly maps the physical representation of the ease to the visual graph.
