# SVG Mask Reveal
An SVG drawing animation acts as a `clipPath`/`mask`. The `stroke` is set to white, and as `stroke-dashoffset` animates to 0, it dynamically reveals an HTML `<img>` tag underneath the mask.
