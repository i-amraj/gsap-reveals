# Shape Morphing
Without premium plugins, GSAP can still morph SVG shapes natively by animating the `attr: { d: "..." }` property, as long as the start and end paths have the exact same number and type of vector nodes.
