# Draw From Center
To animate an SVG stroke from the center outwards in both directions without a premium plugin, the path is split into two separate `<path>` elements that both start at the center coordinate and draw outwards simultaneously.
