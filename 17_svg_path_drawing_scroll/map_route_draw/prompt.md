# Map Route Draw
A dynamic travel route traces itself over an image of a map on scroll. It uses JavaScript to automatically generate an SVG mask so that the drawn line can be dashed while still revealing smoothly.
