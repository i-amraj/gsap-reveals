# 🤖 AI Master Prompt: Color Invert Stacking (`color_invert_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Color Invert Stacking Cards effect (`color_invert_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a vertical scrolling container with 4 pinned cards.
2. The cards should alternate in background color (e.g., Black -> White -> Black -> White).
3. Use GSAP ScrollTrigger callbacks (`onEnter`, `onEnterBack`) attached to each card's pinning trigger to globally animate the `document.body` background color and text color.
4. When a white card pins over a black card, the entire website background smoothly transitions to white. When a black card pins over it, it transitions back to black.
5. Provide complete HTML5, CSS3, and JS files.
```
