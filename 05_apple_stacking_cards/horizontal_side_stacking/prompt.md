# 🤖 AI Master Prompt: Horizontal Side Stacking (`horizontal_side_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Horizontal Side Stacking Cards effect (`horizontal_side_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a horizontal scrolling section. Since native horizontal scrolling is clunky, use a vertical scroll to drive horizontal movement (a pinned horizontal container).
2. Inside the pinned container, there are 4-5 cards placed side by side.
3. As the user scrolls vertically, the cards move horizontally (translateX).
4. When a card reaches the left edge of the screen, it should STOP moving horizontally (pin horizontally) while the *next* cards continue to move left and slide ON TOP of the stopped card.
5. Provide complete HTML5, CSS3, and JS files.
```
