# 🤖 AI Master Prompt: Lottie Scroll Stacking (`lottie_scroll_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Lottie Scroll Stacking Cards effect (`lottie_scroll_stacking`) using GSAP 3, ScrollTrigger, and LottieFiles.

Requirements:
1. Create a vertical scrolling container with 3 pinned cards.
2. Inside each card, embed a Lottie animation using the `<lottie-player>` web component or `lottie-web` script.
3. Use GSAP ScrollTrigger to hook into the `onUpdate` callback of the pinning trigger.
4. Tie the scroll progress (`self.progress`) directly to the Lottie animation's frame, so the animation scrubs forwards and backwards perfectly as the user scrolls through that specific card.
5. Provide complete HTML5, CSS3, and JS files.
```
