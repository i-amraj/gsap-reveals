# 🤖 AI Master Prompt: Explode Away Stacking (`explode_away_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Explode Away Stacking Cards effect (`explode_away_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a vertical scrolling container with 4 pinned cards.
2. Normally, when the next card scrolls up, the pinned card scales down and darkens, staying underneath.
3. Instead of staying underneath, animate the current pinned card aggressively scaling UP (e.g. `scale: 1.5`), adding heavy `blur()`, and fading `opacity` to 0. 
4. This creates a "burst" or "explode" illusion where the old card shatters/dissipates outward to make room for the new card coming from below.
5. Provide complete HTML5, CSS3, and JS files.
```
