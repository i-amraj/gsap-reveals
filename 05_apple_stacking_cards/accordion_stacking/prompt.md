# 🤖 AI Master Prompt: Accordion Stacking (`accordion_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create an Accordion Stacking Cards effect (`accordion_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a vertical scrolling container with 4-5 cards.
2. Unlike the classic pinning where cards stack exactly on `top top`, each subsequent card should pin slightly lower than the previous one (e.g. Card 1 pins at `top top`, Card 2 pins at `top 80px`, Card 3 at `top 160px`).
3. This creates a visible "tab" or accordion effect at the top of the screen as you scroll down.
4. Scale down the previous cards slightly to enhance the depth.
5. Provide complete HTML5, CSS3, and JS files.
```
