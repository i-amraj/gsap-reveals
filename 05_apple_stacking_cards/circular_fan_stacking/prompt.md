# 🤖 AI Master Prompt: Circular Fan Stacking (`circular_fan_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Circular Fan Stacking Cards effect (`circular_fan_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a vertical scrolling container with 4 cards.
2. The cards do not just slide straight up. They are already pinned in the center but start rotated off-screen (e.g. `rotation: 90`, `x: 100%`) or they slide up WHILE rotating.
3. As the user scrolls, animate the next card swinging in like a fan from alternating sides (Card 2 swings in from left rotating right, Card 3 swings in from right rotating left).
4. Provide complete HTML5, CSS3, and JS files.
```
