# 🤖 AI Master Prompt: Blur Depth Stacking (`blur_depth_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Blur Depth Stacking Cards effect (`blur_depth_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a vertical scrolling container with 4 pinned content cards.
2. When the next card scrolls up and covers the current pinned card, animate the pinned card scaling down slightly AND adding a strong CSS `blur()` to it (e.g., `filter: blur(10px) brightness(0.6)`).
3. This creates a camera depth of field effect where cards fall out of focus as they recede into the stack.
4. Provide complete HTML5, CSS3, and JS files.
```
