# 🤖 AI Master Prompt: Snap Scroll Stacking (`snap_scroll_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Snap Scroll Stacking Cards effect (`snap_scroll_stacking`) using GSAP 3, ScrollTrigger, and CSS Scroll Snapping.

Requirements:
1. Create a vertical scrolling container with 4 pinned cards.
2. Enable CSS native scroll snapping on the wrapper (`scroll-snap-type: y mandatory`) and snap alignment on the containers (`scroll-snap-align: start`).
3. Add GSAP ScrollTrigger to handle the visual scaling/darkening of the cards underneath.
4. This ensures the user cannot stop scrolling halfway between two cards; the browser will always snap perfectly to the next or previous card, making the stack feel like a strict slideshow.
5. Provide complete HTML5, CSS3, and JS files.
```
