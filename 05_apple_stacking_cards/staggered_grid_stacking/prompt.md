# 🤖 AI Master Prompt: Staggered Grid Stacking (`staggered_grid_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Staggered Grid Stacking Cards effect (`staggered_grid_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a vertical scrolling container with 3 "grid containers".
2. Inside each grid container, there should be a CSS Grid layout (2x2) with 4 smaller cards.
3. The grid containers pin at the top as the user scrolls.
4. When a grid container is covered by the next one, animate its 4 inner cards scaling down and fading out WITH a stagger (e.g. `stagger: 0.1`) so they disappear piece-by-piece rather than as one huge block.
5. Provide complete HTML5, CSS3, and JS files.
```
