# 🤖 AI Master Prompt: Glassmorphism Stacking (`glassmorphism_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Glassmorphism Stacking Cards effect (`glassmorphism_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a vertical scrolling container with 4 pinned cards.
2. The cards must be partially transparent with CSS `backdrop-filter: blur(20px)` and semi-transparent borders to look like frosted glass.
3. Put a vibrant animated background (e.g. rotating gradient blobs) behind the entire container.
4. As the cards stack on top of each other, their backdrop filters compound, creating an awesome overlapping glass effect where you can vaguely see the text of the card below.
5. Provide complete HTML5, CSS3, and JS files.
```
