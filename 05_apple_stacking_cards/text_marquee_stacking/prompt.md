# 🤖 AI Master Prompt: Text Marquee Stacking (`text_marquee_stacking`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Text Marquee Stacking Cards effect (`text_marquee_stacking`) using GSAP 3 and ScrollTrigger.

Requirements:
1. Create a vertical scrolling container with 3 pinned cards.
2. Inside each card, add a large text marquee (`<div class="marquee">`) that loops infinitely on the X-axis using a GSAP timeline.
3. When the user scrolls, calculate the scroll velocity using ScrollTrigger or simply map the ScrollTrigger progress to the marquee timeline's `timeScale()`.
4. As the cards stack and recede, the text marquee inside them should speed up temporarily during the scroll event.
5. Provide complete HTML5, CSS3, and JS files.
```
