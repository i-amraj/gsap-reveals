# MacBook Open Scroll
Uses CSS 3D transforms (`perspective`, `rotateX`, `transform-origin`) to create a 3D laptop. The laptop lid is initially closed (`rotationX: -90deg`). As the user scrolls, the GSAP timeline opens the lid and reveals the screen contents, a staple of MacBook landing pages.
