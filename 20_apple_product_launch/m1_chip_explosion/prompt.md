# M1 Chip Explosion
Uses CSS `perspective` and isometric 3D rotation (`rotateX(60deg) rotateZ(45deg)`) to draw three layers of a silicon chip stacked tightly via `translateZ`. On scroll, GSAP pulls the Z values far apart, visually "exploding" the chip layers while side stats fade in.
