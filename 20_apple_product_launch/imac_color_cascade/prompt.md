# iMac Color Cascade
A horizontal staggered reveal of 7 differently colored devices. Uses `stagger` on scrub to create a cascading wave effect, combined with a `mix-blend-mode: difference` typography overlay that dynamically reacts to the colorful devices passing beneath it.
