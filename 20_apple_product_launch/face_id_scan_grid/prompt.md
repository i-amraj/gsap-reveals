# Face ID Scan Grid
Simulates the TrueDepth camera dot projector. Uses JavaScript to construct a dense 20x20 CSS grid of 400 dots. On scroll, a GSAP grid stagger radiating from `center` alters the opacity and `translateZ` depth of each individual dot using randomized mathematical functions, generating a 3D topographical map effect.
