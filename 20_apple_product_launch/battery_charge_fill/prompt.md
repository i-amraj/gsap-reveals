# Battery Charge Fill
A massive, glowing CSS representation of the iOS battery icon. On scroll, GSAP simultaneously animates the `width` of the green fill element from `0%` to `100%` and uses a proxy object with an `onUpdate` callback to cleanly animate a typography counter from `0%` to `100%`.
