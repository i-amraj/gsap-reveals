# HomePod Mesh Texture
Uses a pure CSS `radial-gradient` pattern to create an acoustic mesh fabric texture. As the user scrubs the scrollbar, the pattern scales up by `10x`, acting as an extreme macro camera zoom, while a multicolored Siri-style radial glow expands over the mesh to reveal the typography.
