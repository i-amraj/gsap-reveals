# Siri Waveform Scroll
Simulates the Siri interaction waveform. Uses an array of DOM elements styled as glowing dots. On scroll, GSAP's function-based values (`height: () => Math.random()`) are used to independently stretch the dots into vertical bars, creating a dynamic audio waveform effect.
