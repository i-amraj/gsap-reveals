# Safari Tabs Cascade
Recreates the macOS Safari tab view feature. Uses `perspective` to initialize several browser mockups far back along the Z-axis (`translateZ: -1000`). On scrub, GSAP staggers them forward to `z: 0` while adjusting X and Y to create a neat, cascading stack of windows.
