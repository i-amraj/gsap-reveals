# Apple Pencil Draw Scroll
Combines SVG path drawing (`strokeDashoffset`) with GSAP's `MotionPathPlugin`. As the user scrolls, a CSS-styled Apple Pencil accurately follows the bezier curves of an SVG path, rotating dynamically (`autoRotate: true`), leaving a drawn stroke behind it just like the iPad Pro landing page.
