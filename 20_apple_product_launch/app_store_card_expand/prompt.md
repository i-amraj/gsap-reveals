# App Store Card Expand
Simulates the iOS App Store "App of the Day" transition. A standard card layout pins to the screen. As the user scrubs, the width and height animate to `100vw` and `100vh`, the border-radius snaps to `0px`, the image adjusts height, and hidden body text gracefully fades in from below, completely altering the layout.
