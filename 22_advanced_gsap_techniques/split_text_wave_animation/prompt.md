# Split Text Wave Animation
Demonstrates granular typography control without paid plugins. The text characters are wrapped in inline-block `<span>` tags. A single GSAP tween targets all characters, shifting their `y` axis with a `stagger` value. `yoyo: true` and `repeat: -1` create a continuous sine-wave oscillation loop.
