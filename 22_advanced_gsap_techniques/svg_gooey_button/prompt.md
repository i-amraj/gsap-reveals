# SVG Gooey Button
Combines a complex SVG `<feColorMatrix>` filter applied to a parent container with JS-driven particle generation. When hovered, absolute positioned DOM nodes (bubbles) are spawned and animated outward via GSAP. Because they exist under the filter, they stretch and snap organically away from the main button body like pulling apart liquid mercury.
