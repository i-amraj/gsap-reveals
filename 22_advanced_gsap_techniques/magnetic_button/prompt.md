# Magnetic Button
Uses `gsap.quickTo()` for ultra-high performance tracking. When hovering over the button area, the relative mouse coordinates are calculated. The button shifts slightly towards the mouse, while the text inside shifts even further, creating a compelling magnetic parallax interaction.
