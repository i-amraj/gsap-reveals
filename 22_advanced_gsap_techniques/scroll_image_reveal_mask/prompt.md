# Scroll Image Reveal Mask
An essential editorial design pattern. Uses CSS `clip-path: inset(...)` as a highly performant masking technique. As the user scrolls into view, the `clip-path` expands to reveal the full container, while simultaneously the `<img/>` inside scales down from `1.5` to `1`. This opposite scaling creates a strong parallax depth effect.
