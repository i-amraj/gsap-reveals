# 🤖 AI Master Prompt: Zoom Blur Portal (`zoom_blur_portal`)

> **Copy & paste this prompt into any AI agent (Antigravity, ChatGPT, Claude) to implement this exact animation on any website:**

```text
Create a Zoom Blur Portal preloader (`zoom_blur_portal`) using GSAP 3.

Requirements:
1. Setup a `.preloader-wrapper` (fixed, full screen). Give it a solid background.
2. Place a `.counter-container` with text dead center inside it.
3. In JS, run a GSAP counter to 100%. Fade it out when complete.
4. Animate the entire `.preloader-wrapper` directly into the camera: `scale: 10`, `opacity: 0`, and apply a `filter: "blur(30px)"`. 
5. Use `duration: 1.5` and `ease: "power3.in"`. This creates a sensation of physically flying forward through the preloader portal to reach the hero section behind it.
6. Provide complete HTML5, CSS3, and JS files. Ensure the GSAP CDN is included.
```
