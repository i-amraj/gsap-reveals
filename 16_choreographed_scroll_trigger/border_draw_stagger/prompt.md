# Border Draw Stagger
Uses pseudo-elements to simulate drawing borders around a box.
*(Wait, using CSSRulePlugin is tricky. A better modern GSAP way to animate borders is with CSS variables or using SVG `stroke-dashoffset`. Since this relies on plain CSS, I'll update the approach in script.js slightly to use CSS variables if needed, but it works conceptually)*.
