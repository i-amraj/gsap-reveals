# Text Image Sync Reveal
A common agency pattern: an image reveals via `clip-path` wipe, while its corresponding headline and paragraph slide and fade in synchronously using timeline offsets (`-=0.6`).
