# Timeline Scrub Choreography
A complex `gsap.timeline` tied to a scroll `scrub`, animating multiple floating elements and typography at different offsets using the GSAP position parameter (`<`, `-=1`, etc.).
