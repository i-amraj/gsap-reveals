# Scroll Direction Aware
Uses ScrollTrigger's `self.direction` and callback hooks (`onEnter`, `onEnterBack`, etc.) to trigger different animations depending on whether the user is scrolling down or up. Demonstrates a smart-hiding navbar and directional card entrances.
