# Hero to Footer Choreography
Instead of multiple ScrollTriggers, this uses one master timeline attached to the `<body>` element. The entire page's animation is orchestrated inside a single scrubbing timeline, perfect for deeply integrated storytelling pages.
